describe('Article', () => {
  test('renders title', () => {})
  test('calls store for publishing state switcher', () => {})
  test('opens edit page', () => {})
  test('opens modal for deleting', () => {})
  test('calls store for http to delete', () => {})
})