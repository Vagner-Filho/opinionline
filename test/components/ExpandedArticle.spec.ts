describe('Expanded Article', () => {
  test('render cover', () => {})
  test('render tags', () => {})
  test('render title', () => {})
  test('render author picture', () => {})
  test('render full article', () => {})
  test('render date', () => {})
})